1) ITK-Architecture zip pack does NOT include wDocs folder
2) wDoc always retain the latest version as there no version control system is in place
3) Once the word docs are converted into pdf, manually cpy them into Docs folder
4) Core folder contains all the html files used for package navigation, exception index.html
5) Put all the static images here \Template\images

Note:

Content of the release and sub-release descriptions are out of any version control.
We supply the text to the TRUD team in the form of relese document only.